# Demo: Mixed Precision Training

Use `mixed-precision.ipynb` for the course demo. CUDA uses float16 autocasting
and gradient scaling; CPU uses bfloat16 autocasting and does not require
scaling. The companion Python file is preserved as a standalone reference.
